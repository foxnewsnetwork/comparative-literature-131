<% if user_signed_in? %>
	<h1>Create a new manga!</h1>
	<% form_for @manga, :html => { :multipart => true } do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title %>
		<%= f.submit :submit %>
	<% end %>
<% else %>
	<h1>Please sign in first buddy!</h1>
<% end %>
